[package]
name = "yt2pt-application"
version.workspace = true
edition.workspace = true

[dependencies]
yt2pt-domain = { path = "../domain" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
